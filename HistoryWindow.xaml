<Window x:Class="TranscribeWin.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="歷史紀錄" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgPrimaryBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="TitleText" Text="🗂️ 歷史紀錄" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnRefresh" Content="🔄 重新整理" Style="{StaticResource SecondaryButton}" Padding="10,6" Margin="0,0,8,0" Click="BtnRefresh_Click"/>
                <Button x:Name="BtnClear" Content="🗑️ 清除紀錄" Style="{StaticResource SecondaryButton}" Padding="10,6" Click="BtnClear_Click"/>
            </StackPanel>
        </Grid>

        <TextBox x:Name="TxtHistory" Grid.Row="1"
                 Style="{StaticResource DarkTextBox}"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" IsReadOnly="True"
                 Foreground="{StaticResource TextPrimaryBrush}"
                 Padding="12"/>
    </Grid>
</Window>
